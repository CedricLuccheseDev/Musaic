services:
  analyzer:
    build:
      context: .
      dockerfile: Dockerfile.analyzer
    ports:
      - "8000:8000"
    env_file:
      - ./apps/analyzer/.env
    volumes:
      - ./apps/analyzer/app:/app/app
      - ./apps/analyzer/models:/app/models
    restart: unless-stopped
